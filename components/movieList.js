import { View, Text } from 'react-native'
import React from 'react'
import { styles } from '../theme'
import { ViewStyle } from 'react-native';

export default function MovieList({title, data}) {
    return (
        <view className="mb-8 s">
            <text>MovieList</text>
        </view>
    )
}